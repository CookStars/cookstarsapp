import {} from '../reducers';

const initialState = {
  recipeList: {},
};

const recipeReducer = (state = initialState, action) => {
  switch (action.type) {
    //
    //
    default:
      return state;
  }
};

export default recipeReducer;
